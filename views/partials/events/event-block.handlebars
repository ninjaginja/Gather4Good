<div class="row">
<div class="card horizontal hoverable">
<div class="card-stacked">
  <div class="card-content">
    <!-- Title, Location, and Cause -->
    <h4>{{title}} | <span class="location"> {{location_city}}, {{location_state}}</span><div class="chip right cause" value="{{CauseId}}">Gun Violence</div></h4>
    <!-- Date and Time -->
    <h5 id='date'>Date: {{date}} @ <span id='time'> {{time}}</span> </h5>
    <!-- Description -->
    <p>{{description}}</p>
    <!-- Address -->
    <h6 class="right-align">Location: <br><span class="address">{{location_name}}, {{location_street}}, {{location_city}}, {{location_state}} {{location_zip}}</span></h6>
  </div>
  <div class="card-action">
    <a href="#">More Info</a>
  </div>
</div>
</div>
</div>
